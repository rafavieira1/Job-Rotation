#include <stdio.h>

int main(void) {
  int indice = 13, soma = 0, K = 0;

while(K < indice) {
K = K + 1;
soma = soma + K;
}

printf("%d", soma);

//Ao final do processamento, qual será o valor da variável SOMA?

  return 0;
}
